import React from 'react'
import styles from './styles.module'
import { Icons } from '@linkdrop/ui-kit'

const MoonpayWidget = ({ onClose }) => {
  return <div className={styles.container} />
}

export default MoonpayWidget
